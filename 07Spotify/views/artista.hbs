<div>
    {{#each artistas}}
    <h2>{{this.name}}</h2>
        <img src="{{this.images.[0].url}}" alt="">
        <a href="/artista/{{this.id}}"><button>Ver albumes </button></a>
    {{/each}}
</div>