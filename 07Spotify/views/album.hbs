<div>
    {{#each albumes}}
    <h2>{{this.name}}</h2>
        <img src="{{this.images.[0].url}}" alt="">
        <a href="/canciones/{{this.id}}"><button>Ver canciones </button></a>
    {{/each}}
</div>